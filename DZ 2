n= int(input())#
D = dict()
D={}#создали пустой список
for i in range(n):# считываем в словарь, встречая новое название фильма, создаем новую ячейку. Ключ - название фильма, значение - количество ее считываний, то есть голосов за нее
    x = input()
    if x in D:
        D[x] += 1
    else:
        D[x] = 1
k = int(input())#топ сколько фильмов нужно выдать
rating = [(key, D[key]) for key in D]#преобразовали словарь в список

rating.sort(key=lambda x: x[1], reverse=True)#отсортировали по возрастанию, перевернули, получилось по убыванию

for i in range(k):#взяли первые к фильмов топа
    print(rating[i][1])
